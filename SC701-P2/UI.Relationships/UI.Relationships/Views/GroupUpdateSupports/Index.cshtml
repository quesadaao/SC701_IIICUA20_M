@model IEnumerable<UI.Relationships.Models.GroupUpdateSupport>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpdateSupportedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupUpdate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupUserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdateSupportedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupUpdate.GroupUpdateId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupUpdateSupportId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupUpdateSupportId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupUpdateSupportId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
